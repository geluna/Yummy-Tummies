<%= form_for(@account) do |f| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 



  <div class="field">
	<b><font color='32CD32'>Deposit Amount</font></b><br>
	<%= f.text_field :credit, :autocomplete => :off %>
  
  <div class="field">
    <font color="32CD32"><%= f.label :depotype, "Select Deposit Method" %></font><br>
    <%= f.select :depotype, Account::DEPOSIT_TYPES, prompt: 'Select a deposit method'  %>
  </div>

  <div class="actions">
  <%= f.submit 'Deposit Funds',:class => "btn btn-primary btn-sm" %>
  </div>
  <% end %>




