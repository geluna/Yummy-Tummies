<div class="container">
<p id="notice"><%= notice %></p>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Credit</th>
      <th>Debit</th>
      <th>Balance</th>
      <th>Add Fund</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>

      <tr>
        <td><%= @acct.credit %></td>
        <td><%= @acct.debit %></td>
        <td><%= @acct.balance %></td>
        <td><%= link_to 'Add Fund', new_acct_path, :class => "btn btn-primary btn-sm" %></td>
        
      </tr>

  </tbody>
</table>

<%= link_to 'Edit', edit_acct_path(@acct) %> |
<%= link_to 'Back', accts_path %>


</div>
