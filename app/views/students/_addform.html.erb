
<%= form_for(@student) do |f| %>
  <% if @student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% @student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
	<%= f.collection_select(:school_id, School.all, :id, :name) %>
  </div>
  <div class="field">
   <font color="539DC2"> <%= f.label :id %></font><br>
    <%= f.text_field :id %>
  </div>
  <div class="field">
   <font color="539DC2"> <%= f.label :fname %></font><br>
    <%= f.text_field :fname %>
  </div>
  <div class="field">
   <font color="539DC2"> <%= f.label :lname %></font><br>
    <%= f.text_field :lname %>
  </div>

  <div class="actions">
    <%= f.submit :class => "btn btn-primary btn-sm" %>
  </div>
<% end %>
